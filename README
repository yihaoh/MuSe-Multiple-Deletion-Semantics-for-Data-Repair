To run the web app (currently in debug mode):

1. Install packages:
	pip3 install -r requirements.txt

2. Start virtual environment:
	source ./env/bin/activate

3. Run the app:
	python3 app.py

4. Open browser, go to localhost:5000


